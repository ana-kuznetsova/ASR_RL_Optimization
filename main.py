def main(data, dim):
    #initialize
    w = np.zeros((1, dim))
    policy = np.zeros((dim, num_actions))
    
    time_steps = np.arange(len(data))
    for t in time_steps:
        #sample index
        #sample batch
        #load check point
        #train network
        #Compute gain
        #run RL algo
        #update W
        pass
            